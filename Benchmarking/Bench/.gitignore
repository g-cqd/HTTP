# Load-test artifacts and any reference-server build output — regenerated by run.sh, never committed.
results/
*.pid
*.pem        # self-signed TLS material generated for the HTTP2=1 run — never commit keys/certs
hummingbird/.build/
