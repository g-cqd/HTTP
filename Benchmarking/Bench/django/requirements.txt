# Django comparison harness — Python deps. Installed into Benchmarking/Django/.venv by run.sh.
# uvicorn[standard] pulls uvloop + httptools, the fast ASGI path — the fair, production-shaped async
# stack to weigh against the Swift server.
django>=6.0,<6.1
gunicorn>=23.0
uvicorn[standard]>=0.34
