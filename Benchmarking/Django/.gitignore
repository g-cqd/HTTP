# Load-test artifacts, the Python virtualenv, and build output — all regenerated by run.sh, never committed.
results/
.venv/
*.pid

# Swift build output for the ours-bench subject (also directed to /tmp/swiftpm-build/ours-bench).
ours/.build/

# Python bytecode / caches.
__pycache__/
*.py[cod]
