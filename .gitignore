## Swift Package Manager
.build/
.swiftpm/
Package.resolved.tmp
# Reusable library: consumers resolve their own dependency versions.
Package.resolved

## Xcode
*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/

## Editors
.vscode/

## Claude Code agent state (settings, skills, and isolated worktrees — never committed)
.claude/

## macOS
.DS_Store

## Build / scratch (we direct artifacts to /tmp/swiftpm-build/HTTP)
/tmp/swiftpm-build/HTTP/

## Fuzzing corpora artifacts
*.profraw
crash-*
timeout-*
oom-*
